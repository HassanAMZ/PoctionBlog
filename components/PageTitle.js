export default function PageTitle({ children }) {
  return (
    <h1 className="text-5xl font-extrabold leading-none tracking-tight text-gray-900 dark:text-gray-100  sm:text-6xl">
      {children}
    </h1>
  )
}
